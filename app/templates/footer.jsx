/** @jsx React.DOM */
define( [ "react" ], function( React ) {
	var Footer = React.createClass( {
		render : function() {
			return (
				<p>
					Copyright goes here
				</p>
			);
		}
	} );

	return Footer;
} );
